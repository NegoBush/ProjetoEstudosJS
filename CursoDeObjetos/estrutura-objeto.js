const estudante = {
    nome: 'Estevan Wesley',
    idade: 30,
    cpf: '7020133445',
    turma: 'JavaScript'
}

// console.log(estudante.nome);
console.log(`o nome do estudante é ${estudante.nome}`);
console.log(`os três ultimos numeros do CPF são: ${estudante.cpf.substring(0,3)}`);
